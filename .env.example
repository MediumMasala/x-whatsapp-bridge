# X → WhatsApp Bridge Environment Variables

# Required: WhatsApp number in E.164 format WITHOUT the + prefix
# Example: 14155552671 (US) or 919876543210 (India)
WHATSAPP_NUMBER=919224262682

# Optional: PostgreSQL connection string for production
# If not set, SQLite will be used (stored in ./data/clicks.db)
# DATABASE_URL=postgresql://user:password@host:5432/database

# Optional: Admin token for accessing /api/click/:cid endpoint
# Generate a random string: openssl rand -hex 32
ADMIN_TOKEN=your-secret-admin-token-here

# Optional: Salt for IP hashing (for privacy)
# Generate a random string: openssl rand -hex 16
IP_HASH_SALT=your-random-salt-here

# Optional: Node environment
NODE_ENV=development
